node:                 k1
signal:               heap_free
baseline_s:           3          # clean window before we inject
leak_bytes_per_tick:  128        # the fault strength
tick_hz:              10         # firmware sends every 100 ms
run_s:                12         # total episode length
# true_ttf = heap_free / (leak_bytes_per_tick * tick_hz)