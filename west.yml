# west.yml — T2 workspace manifest (this repo = manifest repo).
# Usage: west init -l . && west update

manifest:
  remotes:
    - name: zephyrproject-rtos
      url-base: https://github.com/zephyrproject-rtos

  projects:
    - name: zephyr
      remote: zephyrproject-rtos
      revision: v4.2.0
      import:
        name-allowlist:
          - cmsis
          - cmsis_6
          - hal_nxp
          - tflite-micro

  self:
    path: Zephyr-Renode-S32K-sim