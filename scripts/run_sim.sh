# run_sim.sh — launch the bench: renode sim/renode/boot_topology.resc
# (add --console --disable-gui for headless/CI). Optional flag to run
# sanity_shell.resc instead when no local build exists yet.
