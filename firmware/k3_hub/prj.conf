# prj.conf — K3 hub config: CONFIG_CAN=y, CONFIG_CAN_MAX_FILTER=8,
# CONFIG_PRINTK=y, CONFIG_LOG=n (deterministic console). RX via message
# queue drained in thread context — never log telemetry from ISR.
