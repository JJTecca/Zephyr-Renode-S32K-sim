# Kconfig — app option SDV_NODE_ID (1=powertrain, 2=chassis, 3=body,
# 4=acoustic) so one source tree builds every K1 instance:
#   west build ... -d build/k1_chassis -- -DCONFIG_SDV_NODE_ID=2
# Then sources Kconfig.zephyr.
