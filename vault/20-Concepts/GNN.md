---
tags: [concept]
aliases: [Graph Neural Network, graph neural network]
---
# GNN

Graph neural network; message passing over the ECU/service dependency graph to forecast [[fault propagation]]. Cloud-side PREDICT model, built with [[PyTorch Geometric]].
