---
tags: [concept]
---
# autoencoder

Neural network trained to reconstruct its input through a bottleneck; the [[reconstruction error]] on unseen patterns is our anomaly score. See [[denoising autoencoder]]. Used in DETECT on [[S32K3]].
